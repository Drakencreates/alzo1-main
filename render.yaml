services:
  - type: web
    name: alzo-backend
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        value: secret
